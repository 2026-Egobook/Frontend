<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Egobook" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Egobook" parent="Base.Theme.Egobook" />

    <style name="Theme.SeeMeet.TransparentStatusBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!--drawerlayout statusbar -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="AppTheme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/empty_splash</item>  <!-- 투명 -->
        <item name="android:windowSplashScreenAnimationDuration" tools:targetApi="31">1000</item>
        <item name="postSplashScreenTheme" tools:targetApi="31">@style/Theme.Egobook</item>
    </style>

</resources>
