<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/layer_white"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.square.view.FriendsListFragment">

    <ImageView
        android:id="@+id/iv_friends_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_friends"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="친구 3/10명"
        android:layout_marginStart="4dp"
        app:layout_constraintStart_toEndOf="@id/iv_friends_list"
        app:layout_constraintTop_toTopOf="@id/iv_friends_list"
        app:layout_constraintBottom_toBottomOf="@id/iv_friends_list"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_friends_list"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/iv_friends_list"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_square_friend_list"/>

</androidx.constraintlayout.widget.ConstraintLayout>