# AGENTS.md

> **필수 준수:** 모든 작업은 아래 "작업 흐름" 단계를 반드시 순서대로 따른다. 어떤 작업이든 예외 없이 적용한다.

## 인덱스

- [코드 품질 기준](docs/code-quality-standards.md)
- [코드 리뷰 프롬프트](docs/code-review-prompt.md)

## 개발 방향

- Now in Android, 공식 Android 샘플, Android 권장 아키텍처 같은 신뢰할 수 있는 레퍼런스의 베스트 프랙티스를 우선 참고한다.
- 이 프로젝트는 현재 XML 기반 View 시스템을 사용하므로 Compose 패턴을 그대로 가져오지 말고, XML/View 시스템에 대응되는 방식으로 적용한다.

## 작업 흐름

1. 요구사항 수집
   - 계획의 모든 측면에 대해 공유된 이해에 도달할 때까지 집요하게 질문한다.
   - 설계 의사결정 트리의 각 갈래를 따라가며, 결정 사이의 의존성을 하나씩 해소한다.
   - 각 질문에는 권장 답변을 함께 제시한다.
   - 질문은 한 번에 하나씩만 한다.
   - 코드베이스를 탐색해서 답할 수 있는 질문이라면 사용자에게 묻지 말고 코드베이스를 먼저 탐색한다.
   - UI 작업이라면 목표 UI 스크린샷을 요구한다.

2. TDD로 핵심 로직 구현
   - 핵심 로직은 실패하는 테스트를 먼저 작성한 뒤 구현한다.
   - UI 렌더링 자체보다 분리 가능한 상태 계산, 매핑, 정책, 검증 로직을 우선 테스트한다.

3. 코드 리뷰와 개선
   - [code-review-prompt.md](docs/code-review-prompt.md)의 페르소나 3명을 **실제 병렬 에이전트**로 동시에 실행한다.
   - 각 에이전트는 서로의 컨텍스트를 공유하지 않는 독립된 인스턴스여야 한다. 한 응답 안에서 3개의 Agent 호출을 동시에 보내는 방식으로 실행한다.
   - 각 에이전트에게는 리뷰 대상 코드 + 해당 페르소나(Kent Beck / Martin Fowler / Jake Wharton)만 전달하며, 다른 에이전트의 의견은 포함하지 않는다.
   - 3개의 에이전트 결과가 모두 반환되면 판정 규칙을 적용한다.
   - 2명 이상이 찬성하지 않으면 작업을 다시 진행한다.
   - 2명 이상이 찬성하면 도출된 개선사항을 적용하고 다음 목표로 이동한다.

4. 커밋
   - 세션 재시작 전에 반드시 변경사항을 커밋한다.
   - 커밋 메시지는 작업 내용을 간결하게 요약한다.

5. 세션 재시작
   - 한 작업이 끝나면 세션을 다시 시작한다.
   - Codex에서는 `/new`를 사용한다.
   - Claude Code에서는 `/clear`를 사용한다.
